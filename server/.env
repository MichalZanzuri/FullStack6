# MySQL connection (matches docker-compose.yml).
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=pulse
DB_PASSWORD=pulsepass
DB_NAME=pulse

# HTTP server
PORT=4000

# Bcrypt cost factor for password hashing.
BCRYPT_ROUNDS=10
